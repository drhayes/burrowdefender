// actions.js
//
// Framework for all things actiony.

(function(global, $) {
  
  loki.modules.actions = function(env) {
    env.actionmanager = function() {
      var that = {};
      
      return that;
    }
  };
  
}(this, jQuery));